export const strProp = (desc: string) => ({ type: 'string', description: desc });
export const intProp = (desc: string) => ({ type: 'integer', description: desc });
export const boolProp = (desc: string) => ({ type: 'boolean', description: desc });
