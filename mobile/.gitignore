node_modules/
dist/
dist-ssr/
*.local

# Capacitor native projects (regenerated via `cap add` / `cap sync`)
android/
ios/

# Editor / OS
.DS_Store
*.log
.vite/

# TypeScript incremental build cache
*.tsbuildinfo
